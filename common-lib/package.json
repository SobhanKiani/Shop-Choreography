{
  "name": "@sobhankiani/shopc-common-lib",
  "version": "1.0.10",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "license": "MIT",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del ./build/*",
    "build": "npm run clean && tsc",
    "pub": "npm version patch && npm run build && npm publish --access public"
  },
  "dependencies": {
    "del-cli": "^5.0.0",
    "typescript": "^5.0.4"
  }
}
